from setuptools import setup

setup(name='distributions_probability',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_probability'],
      author = 'Payal Kamboj',
      zip_safe=False)
