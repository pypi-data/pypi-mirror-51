"""Standard django app config for the TOTP app."""

from django.apps import AppConfig


class TOTPConfig(AppConfig):
    """Standard Django App config for the TOTP app."""

    name = "rest_multi_factor.plugins.totp"
