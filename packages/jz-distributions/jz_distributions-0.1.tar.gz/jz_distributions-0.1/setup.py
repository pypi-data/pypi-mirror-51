from setuptools import setup

setup(name='jz_distributions',
      version='0.1',
      description='Gaussian distribution and binomial distribution',
      packages=['jz_distributions'],
      zip_safe=False)
