import argparse
import subprocess
import sys

def activate():
    # TODO
    pass
    # The code below doesn't work.

    # parser = argparse.ArgumentParser(
    #             description='Enter an environment')

    # parser.add_argument('name', help='Name of environment')
    # args = parser.parse_args(sys.argv[2:])
    # print(args.name)
    # subprocess.call("conda activate {}".format(args.name), shell=True)